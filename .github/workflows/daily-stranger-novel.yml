name: Daily Gemini Stranger Novel Chapter to Gist

on:
  # Run daily at 11:00 UTC (different from main novel to avoid conflicts)
  schedule:
    - cron: '0 11 * * *'
  
  # Allow manual triggering
  workflow_dispatch:

jobs:
  generate-chapter:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/stranger-novel/requirements.txt
      
      - name: Generate and publish chapter
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-2.5-flash
          GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
          STRANGER_GIST_ID: ${{ secrets.STRANGER_GIST_ID }}
        run: |
          python scripts/stranger-novel/stranger_novel_daily_to_gist.py
      
      - name: Update config.js with gist ID if missing
        env:
          STRANGER_GIST_ID: ${{ secrets.STRANGER_GIST_ID }}
        run: |
          # Check if gist ID is empty in config.js for indifferent_shore section only
          EMPTY_PATTERN="id: ''"
          if awk "/indifferent_shore/,/^    \}/" config.js | grep -q "$EMPTY_PATTERN"; then
            echo "Updating config.js with STRANGER_GIST_ID for indifferent_shore"
            awk -v gist_id="$STRANGER_GIST_ID" -v pattern="$EMPTY_PATTERN" '
              /indifferent_shore/,/^    \}/ {
                if ($0 ~ pattern) {
                  sub(pattern, "id: '\''" gist_id "'\''")
                }
              }
              {print}
            ' config.js > config.js.tmp && mv config.js.tmp config.js
          else
            echo "Gist ID already set in config.js for indifferent_shore"
          fi
      
      - name: Sync chapters.json to public folder
        run: |
          # Copy chapters.json from docs to public for deployment
          echo "Syncing chapters.json to public/docs/stranger-novel/"
          cp docs/stranger-novel/chapters.json public/docs/stranger-novel/chapters.json
      
      - name: Commit and push updated files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs/stranger-novel/summaries.md docs/stranger-novel/continuity_log.txt docs/stranger-novel/chapters.json public/docs/stranger-novel/chapters.json config.js
          git diff --staged --quiet || git commit -m "Update stranger novel files after Chapter generation"
          git push
